services:
  test_db:
    image: postgres:17
    environment:
      POSTGRES_USER: test
      POSTGRES_PASSWORD: test
      POSTGRES_DB: test_db
    ports:
      - "5433:5432"
    volumes:
      - db_test_data:/var/lib/postgresql/data
    # healthcheck:
    #   test: ["CMD-SHELL", "pg_isready -U user"]
    #   interval: 10s
    #   timeout: 5s
    #   retries: 5

  # test_api:
  #   image: robot-code-api-web
  #   ports:
  #     - "8000:8000"
  #   volumes:
  #     - .:/app
  #   depends_on:
  #     db:
  #       condition: service_healthy
  #   command: bash -c "pip install -r requirements-dev.txt && "

volumes:
  db_test_data: